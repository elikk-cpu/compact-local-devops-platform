# LocalOps API

FastAPI backend for the LocalOps Status Platform.

## Endpoints

- GET /health
- GET /ready
- GET /metrics
- GET /api/status
- GET /api/services
- GET /api/incidents

## Purpose

This API provides the backend layer for the public status page, admin UI, monitoring metrics and future incident management logic.
